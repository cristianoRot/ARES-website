<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ARES - Official Website</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
  <style>
    /* Colors */
    :root {
      --primary-color: #DFBD69; /* Gold */
      --secondary-color: #000000; /* Black */
      --text-color: #FFFFFF; /* White */
    }

    /* Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--secondary-color);
      color: var(--text-color);
    }

    /* Header */
    header {
      background-color: var(--secondary-color);
      border-bottom: 1px solid #333333;
      padding: 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      font-size: 1.8rem;
      font-weight: 900;
      color: var(--primary-color);
    }

    nav {
      display: flex;
      gap: 20px;
    }

    nav a {
      color: var(--text-color);
      text-decoration: none;
      font-weight: 700;
      font-size: 1rem;
      transition: color 0.3s;
    }

    nav a:hover {
      color: var(--primary-color);
    }

    /* Main Container */
    .container {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      padding: 20px;
      max-width: 1200px;
      margin: 0 auto;
    }

    /* Left Section (Thumbnail & Description) */
    .left-section {
      flex: 1 1 60%;
      margin-right: 20px;
    }

    .thumbnail {
      width: 100%;
      border-radius: 8px;
      overflow: hidden;
      margin-bottom: 20px;
    }

    .thumbnail img {
      width: 100%;
      height: auto;
      display: block;
    }

    .description {
      font-size: 1rem;
      line-height: 1.6;
      color: #E5E5E5;
    }

    /* Right Section (Buttons & Info) */
    .right-section {
      flex: 1 1 35%;
      background-color: #111111;
      border: 1px solid #333333;
      border-radius: 8px;
      padding: 20px;
    }

    .info-section {
      margin-bottom: 20px;
    }

    .info-section h2 {
      font-size: 1.5rem;
      color: var(--primary-color);
      margin-bottom: 10px;
    }

    .info-item {
      display: flex;
      justify-content: space-between;
      margin-bottom: 10px;
    }

    .info-item span {
      font-size: 0.9rem;
    }

    .info-label {
      color: var(--primary-color);
      font-weight: 700;
    }

    .info-value {
      color: #E5E5E5;
    }

    /* Download Buttons */
    .cta-group {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    .cta {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 15px;
      font-size: 1rem;
      font-weight: 700;
      color: var(--secondary-color);
      background-color: var(--primary-color);
      border: none;
      border-radius: 8px;
      cursor: pointer;
      text-decoration: none;
      text-align: center;
      transition: background-color 0.3s;
    }

    .cta img {
      margin-right: 10px;
      width: 20px;
      height: auto;
    }

    .cta:hover {
      background-color: #b9954c;
    }

    /* Footer */
    footer {
      background-color: var(--secondary-color);
      border-top: 1px solid #333333;
      text-align: center;
      padding: 20px;
      font-size: 0.9rem;
      color: #777;
    }

    footer a {
      color: var(--primary-color);
      text-decoration: none;
      font-weight: 700;
      transition: color 0.3s;
    }

    footer a:hover {
      color: #b9954c;
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header>
    <div class="logo">ARES</div>
    <nav>
      <a href="#">Home</a>
      <a href="#features">Features</a>
      <a href="#info">Info</a>
      <a href="#contact">Contact</a>
    </nav>
  </header>

  <!-- Main Container -->
  <div class="container">
    <!-- Left Section -->
    <div class="left-section">
      <div class="thumbnail">
        <img src="images/GameThumbnail.png" alt="Game Thumbnail">
      </div>
      <div class="description">
        <p>
          Dive into the world of ARES, a futuristic multiplayer game where you explore, customize, and dominate.
          With stunning graphics and an immersive storyline, ARES promises an unparalleled gaming experience.
        </p>
      </div>
    </div>

    <!-- Right Section -->
    <div class="right-section">
      <div class="info-section">
        <h2>Game Information</h2>
        <div class="info-item">
          <span class="info-label">Developer:</span>
          <span class="info-value">Cristiano Rotunno</span>
        </div>
        <div class="info-item">
          <span class="info-label">Distributor:</span>
          <span class="info-value">ARES Studio</span>
        </div>
        <div class="info-item">
          <span class="info-label">Release Date:</span>
          <span class="info-value">2025</span>
        </div>
        <div class="info-item">
          <span class="info-label">Platforms:</span>
          <span class="info-value">
            <img src="https://img.icons8.com/windows/32/null/windows-10.png" alt="Windows">
            <img src="https://img.icons8.com/ios/32/null/mac-os.png" alt="MacOS">
          </span>
        </div>
      </div>
      <div class="cta-group">
        <a href="https://drive.google.com/uc?export=download&id=macOS-file-id" class="cta">
          <img src="images/apple_logo.png" alt="Apple Logo"> Download for MacOS
        </a>
        <a href="https://drive.google.com/uc?export=download&id=windows-file-id" class="cta">
          <img src="images/win11_logo.png" alt="Windows Logo"> Download for Windows
        </a>
        <a href="https://store.steampowered.com/" class="cta">
          <img src="images/steam_logo.png" alt="Steam Logo"> Get it on Steam
        </a>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer>
    © 2025 ARES. All rights reserved. | <a href="#privacy">Privacy Policy</a>
  </footer>
</body>
</html>
