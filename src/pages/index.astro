<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ARES - Official Website</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
  <style>
    /* Colors */
    :root {
      --primary-color: #DFBD69; /* Gold */
      --secondary-color: #000000; /* Black */
      --text-color: #FFFFFF; /* White */
    }

    /* Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* Body */
    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--secondary-color);
      color: var(--text-color);
    }

    /* Header */
    header {
      background: var(--secondary-color);
      padding: 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .logo {
      font-size: 1.5rem;
      font-weight: 900;
      color: var(--primary-color);
    }
    .nav-links {
      display: flex;
      gap: 15px;
    }
    .nav-links a {
      color: var(--text-color);
      text-decoration: none;
      font-weight: 700;
      transition: color 0.3s;
    }
    .nav-links a:hover {
      color: var(--primary-color);
    }

    /* Hero Section */
    .hero {
      text-align: center;
      padding: 100px 20px;
      background: linear-gradient(to bottom, rgba(0, 0, 0, 0.7), var(--secondary-color)), url('images/GameIcon2.png') center/cover no-repeat;
    }
    .hero h1 {
      font-size: 3rem;
      font-weight: 900;
      margin-bottom: 20px;
      color: var(--primary-color);
    }
    .hero p {
      font-size: 1.2rem;
      margin-bottom: 30px;
      line-height: 1.6;
      color: #E5E5E5;
    }
    .hero .cta-group {
      display: flex;
      justify-content: center;
      gap: 15px;
      margin-top: 30px;
    }
    .hero .cta {
      padding: 15px 30px;
      font-size: 1.2rem;
      font-weight: 700;
      color: var(--secondary-color);
      background-color: var(--primary-color);
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s;
      text-decoration: none;
      display: inline-block;
    }
    .hero .cta:hover {
      background-color: #b9954c; /* Darker gold */
    }

    /* Info Section */

      .features-box {
        padding: 20px;
        max-width: 400px;
        margin: 20px auto;
      }

      .feature-item {
        display: flex;
        justify-content: space-between;
        margin-bottom: 15px;
        border-bottom: 1px solid #333333;
        padding-bottom: 8px;
      }
    
      .feature-label {
        font-weight: 700;
        font-size: 0.9rem;
        color: #DFBD69; /* Gold */
      }
    
      .feature-value {
        font-size: 0.9rem;
        text-align: right;
      }
    
      .feature-value img {
        margin-left: 5px;
        vertical-align: middle;
      }
    
      .action-buttons {
        display: flex;
        justify-content: space-around;
        margin-top: 20px;
      }
    
      .btn {
        background-color: #DFBD69;
        color: #000000;
        border: none;
        padding: 10px 20px;
        font-size: 0.9rem;
        font-weight: 700;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }
    
      .btn:hover {
        background-color: #b9954c;
      }

    /* Footer */
    footer {
      background: var(--secondary-color);
      text-align: center;
      padding: 20px;
      color: #777;
      font-size: 0.9rem;
    }
    footer a {
      color: var(--primary-color);
      text-decoration: none;
      transition: color 0.3s;
    }
    footer a:hover {
      color: #b9954c;
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header>
    <div class="logo">ARES</div>
    <nav class="nav-links">
      <a href="#">Home</a>
      <a href="#features">Features</a>
      <a href="#info">Info</a>
      <a href="#contact">Contact</a>
    </nav>
  </header>

  <!-- Hero Section -->
  <section class="hero">
    <h1>Welcome to ARES</h1>
    <p>
      Discover the future of multiplayer gaming. Explore, customize, fight, and dominate in a futuristic universe with breathtaking graphics.
    </p>
    <div class="cta-group">
      <a href="https://drive.google.com/uc?export=download&id=macOS-file-id" class="cta">Download for MacOS</a>
      <a href="https://drive.google.com/uc?export=download&id=windows-file-id" class="cta">Download for Windows</a>
      <a href="https://store.steampowered.com/" class="cta">Get it on Steam</a>
    </div>
  </section>

  <!-- Info Section -->
  <section class="features-box">
  <div class="feature-item">
    <span class="feature-label">Developer</span>
    <span class="feature-value">Cristiano Rotunno</span>
  </div>
  <div class="feature-item">
    <span class="feature-label">Distributor</span>
    <span class="feature-value">ARES Studio</span>
  </div>
  <div class="feature-item">
    <span class="feature-label">Release Date</span>
    <span class="feature-value">2025</span>
  </div>
  <div class="feature-item">
    <span class="feature-label">Platforms</span>
    <span class="feature-value">
      <img src="https://img.icons8.com/windows/32/null/windows-10.png" alt="Windows">
      <img src="https://img.icons8.com/ios/32/null/mac-os.png" alt="MacOS">
    </span>
  </div>
  <div class="action-buttons">
    <button class="btn">Share</button>
    <button class="btn">Report</button>
  </div>
  </section>

  <!-- Footer -->
  <footer>
    © 2025 ARES. All rights reserved.
  </footer>
</body>
</html>
