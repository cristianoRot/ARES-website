---
import Layout from '../layouts/Layout.astro';

const heroTitle = "ARES";
const subTitle = "TI HANNO COSTRETTO A FUGGIRE\nTI DANNO LA CACCIA";
const description = `
Mentre l'attenzione del mondo è concentrata sulla Guerra del Golfo, un'oscura forza 
clandestina si è infiltrata nelle alte sfere della CIA, dichiarando traditore chiunque le si opponga. 
Esiliato dalla sua agenzia e dal paese che un tempo li considerava eroi, il veterano di Black Ops 
Frank Woods e il suo team si ritrovano braccati dalla stessa macchina militare che li ha creati.
`;
---

<Layout title="ARES">
    <!-- Header Section -->
    <header class="bg-black text-white flex justify-between items-center px-6 py-4">
        <h1 class="text-lg font-bold"ARES <span class="text-orange-600">ARES</span></h1>
        <a href="#" class="btn btn-primary bg-orange-600 px-4 py-2 text-white font-bold rounded">
            DOWNLOAD
        </a>
    </header>

    <!-- Hero Section -->
    <section class="relative bg-cover bg-center text-center text-white" style="background-image: url('../assets/GameIcon2.png'); height: 600px;">
        <div class="absolute inset-0 bg-black bg-opacity-50"></div>
        <div class="relative z-10 flex flex-col items-center justify-center h-full">
            <h1 class="text-5xl font-extrabold uppercase">{heroTitle}</h1>
            <a href="#" class="btn btn-primary bg-orange-600 px-6 py-3 mt-6 text-xl font-bold rounded">
                DOWNLOAD NOW
            </a>
        </div>
    </section>

    <!-- Subsection -->
    <section class="bg-black text-white py-12 px-6 text-center">
        <h2 class="text-3xl font-extrabold text-orange-600 uppercase mb-4">{subTitle}</h2>
        <p class="text-lg leading-relaxed">{description}</p>
    </section>

    <!-- Video Section -->
    <section class="bg-black py-12 px-6">
        <div class="relative max-w-4xl mx-auto">
            <img src="/path-to-thumbnail.jpg" alt="Anteprima del video" class="w-full rounded">
            <a href="#" class="absolute inset-0 flex items-center justify-center">
                <div class="bg-orange-600 p-4 rounded-full">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14.752 11.168l-5.197 3.25A1 1 0 018 13.482V6.518a1 1 0 011.555-.832l5.197 3.25a1 1 0 010 1.664z" />
                    </svg>
                </div>
            </a>
        </div>
    </section>
</Layout>
